# Observability Provider tests

Hermetic tests in this directory exercise Service/Binding ResourceService
separation, stream-only ComponentSession admission, bounded ingress,
configuration, and redaction behavior without starting a collector, broker,
or backend.
